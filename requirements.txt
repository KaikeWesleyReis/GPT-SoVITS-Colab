# Requirements for GPT-SoVITS v2, English-only, CPU-only inference.
#
# Traced directly from the actual import chain exercised by:
#   tts-local-inference.py -> cnhubert, process_ckpt, module.models,
#   AR.models.t2s_lightning_module, text.cleaner (English branch only)
#
# Excludes: gradio (WebUI only), all Chinese/Japanese/Korean/Cantonese text
# processing packages (never imported when language == "en"), training-only
# tooling (funasr, modelscope, peft), and API/server packages (fastapi,
# ffmpeg-python) not touched by this inference path.

torch
torchaudio
numpy
librosa
transformers
pytorch-lightning
torchmetrics
einops
packaging
matplotlib
tqdm
x_transformers
g2p_en
wordsegment
nltk
inflect
